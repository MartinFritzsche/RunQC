# Load required libraries

# Parse binaries from InterOp folder
library(savR)

# Parse runParameters.xml file
library(xml2)

# Data wrangling
library(tidyr)

# Data wrangling
library(dplyr)

# Plotting
library(ggplot2)

# Fonts for plots
library(extrafont)

# Interactive plots
library(plotly)

# Arrange plots in grid
library(ggpubr)